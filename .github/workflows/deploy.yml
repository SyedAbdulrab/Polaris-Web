# Build the Polaris Web image, push to GHCR, THEN deploy — in that order.
#
# Ordered with `needs:` so the VM never pulls a stale web image:
#   build  → builds + pushes ghcr.io/<owner>/polaris-web:{sha,latest}
#            (NEXT_PUBLIC_API_URL is baked in here at build time)
#   deploy → (needs build) SSHes to the VM and runs redeploy.sh (in the polaris
#            repo on the box), which pulls the freshly-pushed images + restarts.
#
# Required repository secrets:
#   EC2_HOST, EC2_USER, EC2_SSH_KEY

name: Build & Deploy Polaris Web

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

concurrency:
  group: polaris-web-release
  cancel-in-progress: false

env:
  # Not a secret — ends up in the public JS bundle. Bump if the API domain changes.
  PUBLIC_API_URL: https://api.abdulrab.store

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/polaris-web
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=${{ env.PUBLIC_API_URL }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build           # deploy only after the image is built + pushed
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy.pem
          chmod 600 ~/.ssh/deploy.pem
          ssh-keyscan -H "${{ secrets.EC2_HOST }}" >> ~/.ssh/known_hosts

      - name: Trigger redeploy on the VM
        run: |
          ssh -i ~/.ssh/deploy.pem \
              -o ServerAliveInterval=20 \
              "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}" \
              "/home/${{ secrets.EC2_USER }}/polaris/deploy/redeploy.sh"
